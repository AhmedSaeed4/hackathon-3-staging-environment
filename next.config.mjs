/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.sanity.io', 'lh3.googleusercontent.com'], // Allow images from this domain
    },
  };
  
  export default nextConfig;
  